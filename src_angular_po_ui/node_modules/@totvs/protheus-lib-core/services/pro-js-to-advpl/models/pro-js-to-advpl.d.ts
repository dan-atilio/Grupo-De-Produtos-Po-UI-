/**
 * @description
 *
 * Interface que representa o objeto de configuração de um observable
 * de um envio de mensagem via jsToAdvpl
 */
export interface ProJsToAdvpl {
    sendInfo?: SendInfo;
    receiveId?: string;
    autoDestruct: boolean;
}
interface SendInfo {
    type: string;
    content?: string;
}
export {};
