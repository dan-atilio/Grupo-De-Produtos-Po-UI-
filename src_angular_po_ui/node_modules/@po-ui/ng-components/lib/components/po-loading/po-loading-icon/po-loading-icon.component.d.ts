import * as i0 from "@angular/core";
/**
 * @docsPrivate
 *
 * @description
 *
 * Componente que exibe um ícone de carregamento de conteúdo. A cor padrão para ele é a primária conforme o tema utilizado.
 * É possível alterá-la para um tom cinza conforme a necessidade.
 */
export declare class PoLoadingIconComponent {
    private _neutralColor;
    private _size;
    id: string;
    /**
     * @optional
     *
     * @description
     *
     * Definição para cor neutra (cinza) para o ícone de carregamento.
     *
     * @default `false`
     */
    set neutralColor(value: boolean);
    get neutralColor(): boolean;
    /**
     * @optional
     *
     * @description
     *
     * Definição do tamanho do ícone.
     *
     * Valores válidos:
     *  - `xs`: tamanho `extra small`
     *  - `sm`: tamanho `small`
     *  - `md`: tamanho `medium`
     *  - `lg`: tamanho `large`
     *
     * @default `md`
     */
    set size(value: string);
    get size(): string;
    static ɵfac: i0.ɵɵFactoryDeclaration<PoLoadingIconComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<PoLoadingIconComponent, "po-loading-icon", never, { "neutralColor": { "alias": "p-neutral-color"; "required": false; }; "size": { "alias": "p-size"; "required": false; }; }, {}, never, never, false, never>;
}
