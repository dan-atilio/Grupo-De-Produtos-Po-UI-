import { OnInit } from '@angular/core';
import * as i0 from "@angular/core";
/**
 * @description
 *
 * Este componente apresenta uma linha demarcadora de blocos e pode conter um *label*. Seu uso é indicado para definição
 * e organização de informações em uma tela e sua característica é semelhante à tag `<hr>`.
 *
 * #### Tokens customizáveis
 *
 * É possível alterar o estilo do componente usando os seguintes tokens (CSS):
 *
 * > Para maiores informações, acesse o guia [Personalizando o Tema Padrão com Tokens CSS](https://po-ui.io/guides/theme-customization).
 *
 * | Propriedade                            | Descrição                                             | Valor Padrão                                    |
 * |----------------------------------------|-------------------------------------------------------|-------------------------------------------------|
 * | **Default Values**                     |                                                       |                                                 |
 * | `--color`                              | Cor principla do divider&nbsp;                        | `var(--color-neutral-mid-40)`                   |
 * | `--stroke-linecap`                     | Extremidade da linha&nbsp;                            | `round`                                         |
 *
 */
export declare class PoDividerBaseComponent implements OnInit {
    coordinateX1: string;
    coordinateX2: string;
    private _borderWidth;
    /** Valor do rótulo a ser exibido. */
    label?: string;
    /**
     * @optional
     *
     * @description
     *
     * Define a espessura da linha.
     *
     * Valores válidos:
     * - small
     * - medium
     * - large
     *
     * @default `small`
     */
    set borderWidth(value: string);
    get borderWidth(): string;
    ngOnInit(): void;
    getCoordinates(): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<PoDividerBaseComponent, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<PoDividerBaseComponent, never, never, { "label": { "alias": "p-label"; "required": false; }; "borderWidth": { "alias": "p-border-width"; "required": false; }; }, {}, never, never, false, never>;
}
